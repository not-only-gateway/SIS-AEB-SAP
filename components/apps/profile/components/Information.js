import {Empty} from "mfc-core";

export default function Information() {
    return (
        <div style={{width: '100%', height: '100%', position: 'relative'}}>
            <Empty customLabel={'Vazio no momento.'}/>
        </div>
    )
}